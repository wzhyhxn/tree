# 校园植物地图 — 设计规范 v1.0

## 设计语言

**自然 × 规范**：自然系绿色色板 + 小程序级组件结构，Phase 1 好看，Phase 2 迁移顺滑。

---

## 色彩系统

| 变量 | 色值 | 用途 |
|------|------|------|
| `--primary` | `#2d6a4f` | 主按钮、导航、强调文字 |
| `--primary-light` | `#52b788` | 悬停态、辅助图标 |
| `--primary-bg` | `#f0fdf4` | 卡片底色、选中背景 |
| `--surface` | `#ffffff` | 卡片、弹窗 |
| `--bg` | `#f5f6f8` | 页面底色 |
| `--text` | `#1a1a2e` | 主文字 |
| `--text-secondary` | `#6b7280` | 辅助文字、拉丁名 |
| `--border` | `#e5e7eb` | 分割线、输入框边框 |
| `--danger` | `#dc2626` | 错误、移除状态 |
| `--warning` | `#f59e0b` | 注意状态 |
| `--radius` | `12px` | 统一圆角 |
| `--shadow` | `0 2px 12px rgba(0,0,0,0.06)` | 卡片阴影 |

---

## 字体层级

| 层级 | 大小 | 字重 | 用途 |
|------|------|------|------|
| H1 | 22px | 700 | 页面标题 |
| H2 | 18px | 600 | 卡片标题 |
| Body | 15px | 400 | 正文 |
| Caption | 13px | 400 | 辅助说明、拉丁名 |
| Small | 12px | 400 | 标签、编号 |

- 中文：系统默认（苹方/PingFang SC, 微软雅黑）
- 拉丁名：Georgia / Times New Roman（斜体）

---

## 组件规范

### 导航条（NavBar）
- 高度 48px，毛玻璃 `backdrop-blur`
- 左侧 Logo 🍃 + "校园植物地图"
- 背景 `rgba(255,255,255,0.85)`
- 底部 1px `--border` 分割线

### 植物信息卡（PlantCard）
- 白色卡片 `--surface`，圆角 `--radius`，阴影 `--shadow`
- 左侧缩略图（若无则渐变色占位）
- 右侧：物种名 H2 + 拉丁名 Caption（斜体灰色）
- 底部：编号 + 位置 + 状态标签

### 状态标签（StatusTag）
- 健康 `#52b788` 浅绿背景
- 异常 `#fef2f2` 浅红背景
- 圆角 20px，字号 12px

### 信息窗（InfoWindow）
- 宽度 200px，圆角 8px
- 植物名 + 编号 + "查看详情 →" 链接
- 地图标记用植物 emoji 🌳🌸🌿

### 悬浮操作按钮（FAB）
- 右下角固定，距离底部 24px
- 圆形 48px，`--primary` 背景，白色 + 图标
- 阴影 0 4px 16px

### 上传区域（UploadZone）
- 虚线边框，居中图标 + "点击或拖拽上传"
- hover 时边框变色

---

## 页面结构

### 首页（HomePage）
```
┌─────────────────────────────┐
│ 🍃 校园植物地图    [管理]   │  ← NavBar
├─────────────────────────────┤
│                             │
│        高德地图              │  ← 全屏地图
│                             │
│   ┌──────────────┐          │
│   │ 🌳 5 棵树    │          │  ← 左下统计卡片
│   │ 📍 2 个校区  │          │
│   └──────────────┘          │
│                        [+]  │  ← FAB
└─────────────────────────────┘
```

### 详情页（PlantDetailPage）
```
┌─────────────────────────────┐
│ ← 返回                      │
├────── 图片轮播 ──────────────┤
│  ┌───────────────────────┐  │
│  │     [植物照片]         │  │
│  └───────────────────────┘  │
├────── 物种信息 ──────────────┤
│  银杏  Ginkgo biloba        │
│  乔木                       │
│  活化石植物，扇形叶...       │
├────── 位置信息 ──────────────┤
│  编号    RD-001             │
│  位置    图书馆东侧          │
│  地址    北京市海淀区...     │
│  状态    ● 健康             │
└─────────────────────────────┘
```

### 管理页（AdminPage）
```
登录态：
┌─────────────────────────────┐
│ ← 返回    新增植物    [退出] │
├─────────────────────────────┤
│  位置描述  [________]       │
│  编号      [________]       │
│  类型      [选择▾]          │
│  拉丁名    [________]       │
│  简介      [________]       │
│  经度      [___] 纬度 [___] │
│  ┌─ 上传图片 ──────────┐   │
│  │   📷 点击上传       │   │
│  └─────────────────────┘   │
│         [提交]              │
└─────────────────────────────┘

未登录：居中卡片式登录框
```

---

## 交互规范

- 页面切换：无动画，即切即显
- 卡片 hover：轻微上移 2px + 阴影加深
- 按钮点击：`scale(0.97)` 微缩反馈
- 信息窗：点击 marker 弹出，再次点击地图关闭
- 上传进度：按钮 loading 态 + 文字提示

---

## 图标方案

使用 emoji 原生图标，零依赖：
- 🌳 树木地图
- 🔍 搜索
- 📷 上传图片
- ⚙️ 管理
- ← → 返回/前进
- ⚠️ 异常状态
- ✅ 健康状态
