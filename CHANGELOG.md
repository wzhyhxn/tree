# 前端重新设计变更记录

## 设计方向：「The Herbarium（植物标本馆）」

从植物学档案与现代制图学中汲取灵感，为中国人民大学校园植物地图构建一套克制、学术感的视觉系统。

---

## 一、全局设计系统（`index.html`）

### 1.1 移除 Tailwind CSS CDN
- 移除 `<script src="https://cdn.tailwindcss.com">`，改为全量自定义 CSS
- 所有视觉决策由一个统一的 token 系统控制

### 1.2 新增 CSS 自定义属性（Design Tokens）

| Token | 值 | 用途 |
|---|---|---|
| `--color-bg` | `#FCFCFA` | 页面背景 |
| `--color-surface` | `#FFFFFF` | 卡片/面板 |
| `--color-text` | `#1A1A1A` | 主文字 |
| `--color-text-secondary` | `#706E6B` | 次要文字 |
| `--color-text-tertiary` | `#9D9A96` | 辅助文字 |
| `--color-accent` | `#1F5C3A` | 主题绿（原 #2d6a4f） |
| `--color-accent-hover` | `#163F28` | 悬停加深 |
| `--color-accent-soft` | `#ECF4EE` | 淡绿背景 |
| `--color-warm` | `#C2852E` | 银杏金（仅用于 CTA 链接） |
| `--color-warm-soft` | `#FDF6EC` | 淡金背景 |
| `--color-border` | `#EBE9E5` | 边框 |
| `--color-border-light` | `#F2F0EC` | 细分隔线 |
| `--color-danger` | `#D14343` | 危险操作 |
| `--color-overlay` | `rgba(18,18,18,0.45)` | 遮罩 |

### 1.3 字体系统

| 角色 | 字体栈 |
|---|---|
| 标题（中文） | `Georgia, 'Noto Serif SC', 'Songti SC', serif` |
| 正文 | `-apple-system, 'PingFang SC', 'Microsoft YaHei', sans-serif` |
| 拉丁名 | `Georgia, 'Times New Roman', serif`（斜体） |
| 等宽/坐标 | `'SF Mono', 'Consolas', monospace` |

### 1.4 通用组件

新增可复用 CSS class：
- `.card` / `.card-elevated` — 卡片容器
- `.btn` / `.btn-primary` / `.btn-ghost` / `.btn-danger` / `.btn-warm` — 按钮系统
- `.input` — 统一输入框
- `.tag` / `.tag-warm` — 标签
- `.nav-blur` — 毛玻璃导航栏
- `.overlay-backdrop` — 遮罩层
- `.skeleton` — 加载骨架屏
- `.upload-zone` — 上传区域
- `.check-circle` / `.check-square` — 自定义复选框
- 自定义滚动条样式

---

## 二、主页（`HomePage.vue`）

### 2.1 导航栏
- 原来：Tailwind utility class 拼出的 header
- 现在：`nav-blur` 毛玻璃效果（`backdrop-filter: blur(12px)`），标题使用衬线体
- 右上角新增季节下拉选择器，代替原来的左下角面板

### 2.2 搜索面板
- 原来：位于左侧的搜索面板
- 现在：移至左上角地图叠加层，搜索框圆角卡片带搜索图标
- 搜索结果改为列表项（emoji + 名称 + 位置），悬浮/选中高亮
- 植物详情迷你卡片：emoji + 名称 + 拉丁分类 + "详情 →" 银杏金色链接

### 2.3 季节选择器
- 原来：左侧面板带编号按钮
- 现在：右上角下拉菜单，选项为「🌸 春花」「🌿 夏荫」「🍂 秋叶」「❄️ 冬枝」

### 2.4 植物详情弹窗
- 原来：底部滑入的灰色背景面板
- 现在：居中模态卡片（`overlay-backdrop`），标题栏 + 图片 + 描述 + 四宫格信息（类型/位置/坐标/编号）

---

## 三、植物详情页（`PlantDetailPage.vue`）

### 3.1 布局
- 原来：图片 + 灰色信息区 + 小地图
- 现在：
  - 顶部 Sticky 导航栏（返回 + 植物名）
  - Hero 大图（16rem 高）
  - 信息卡片：标题/拉丁分类/标签 + 位置信息 + 植物描述
  - 地图卡片：带标题栏的静态小地图（禁用拖拽缩放，仅展示标记 + 半透明绿色圆覆盖）
  - 底部坐标脚注（等宽字体）

### 3.2 加载态
- 新增骨架屏（圆形 + 横条），替代空白

---

## 四、管理后台（`AdminPage.vue`）— 完全重构

### 4.1 背景色
- 从 `var(--color-bg)` (#FCFCFA) 改为 `#F4EFE6`（暖米纸色，护眼）

### 4.2 侧边栏
- 原来：文字列表
- 现在：图标 + 文字，激活项有淡绿背景

### 4.3 登录页
- 卡片风格统一，新增「中国人民大学」标签和「植物地图管理」标题

### 4.4 树木目录
- 原来：通用 `.card` 样式
- 现在：整行条目（白底 + 边框），emoji 前置
- 空状态提示：「暂无数据 — 前往「新增树木」添加第一棵植物」

### 4.5 新增/编辑表单（核心重构）
- **原来布局问题**：所有字段（物种/描述/位置/照片/坐标）挤在一张卡片内，
  使用 `form-two-col` flex 布局 + 地图选择器在右侧并列，当没有地图选择器时卡片宽度突变，
  上传区域太窄
- **现在**：四个独立卡片，纵向堆叠，每张卡片有明确的标题和分隔线：
  1. **物种信息** — 类型输入 + 描述 textarea
  2. **位置信息** — 位置描述 + 经纬度输入（inline 排列）+ "地图选点"按钮
  3. **地图选点**（可展开/收起）— 全宽地图 picker
  4. **植物照片** — 独立的上传区域卡片，虚线边框，拖拽友好
- 提交按钮始终在底部，与反馈信息同行

### 4.6 删除确认弹窗
- 原来：`alert()` 弹窗
- 现在：自定义模态框（`overlay-backdrop`），警告图标 + 植物名 + 取消/确认按钮

### 4.7 季节管理
- 卡片式 grid 布局保持，配色统一到新设计系统

---

## 五、地图组件（`MapView.vue`）

### 5.1 图层切换
- 原来：左上角两个按钮
- 现在：右上角胶囊式切换（卫星/地图），激活项绿底白字

### 5.2 植物标记
- 圆点颜色改为 `#1F5C3A`（主题绿）
- 标签字体加粗，添加白色文字阴影增强可读性

---

## 六、图片上传组件（`ImageUpload.vue`）

- 上传区域使用统一的 `.upload-zone` 样式
- 新增上传中状态「⏳」和预览图
- 压缩参数从 1920px 调整到 1080px

---

## 七、设计决策说明

### 避免的模板化陷阱
| 模板陷阱 | 本设计选择 |
|---|---|
| 暖奶油色 `#F4F1EA` + 赤陶色高亮 | 冷白 `#FCFCFA`，仅用银杏金一处点缀 |
| 全黑背景 + 荧光绿 | 留白为主，深绿克制使用 |
| 报纸式无圆角网格 | 适度圆角（6–14px），现代卡片式 |
| 数字编号 (01/02/03) | 仅当内容本身是序列时使用（如季节） |

### 唯一的视觉风险
**银杏金 `#C2852E` 仅用于详情跳转链接** — 不是所有按钮，不是多处点缀，
而是在整页的绿色/灰色系统中唯一的一抹暖色。这给了页面一个记忆点，
同时不破坏整体的冷静学术调性。
